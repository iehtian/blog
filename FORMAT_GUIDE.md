# 博客文档格式指导（供 AI 优化遵循）

本指南用于统一后续 AI 对博客内容的格式优化与排版输出，确保风格一致、结构清晰、易读易维护。

## 1. 基本原则

- 保持原意不变，避免引入新事实或未经验证的结论。
- 优先精炼、清晰、结构化表达，减少赘述。
- 代码、命令、配置必须可复制且语义准确。
- 不随意更改技术术语、变量名、接口名与路径。
- 如需补充内容，优先以“提示/注意”形式补充。
- 避免使用大段完整示例，仅展示必要内容。

## 2. 统一的文档结构

推荐结构如下（可按需删减）：

1. 标题
2. 核心内容（分段+小标题）
3. 示例/代码块
4. 小结/下一步（可选）

## 3. 标题与层级

- 文章主标题使用 `#`。
- 一级小节使用 `##`，二级小节使用 `###`。
- 避免跳级：`##` 下不要直接用 `####`。
- 标题尽量简洁明确，避免过长。

## 4. 段落与列表

- 每段 2~5 行为佳，避免超长段落。
- 列表项统一使用 `-`，同级列表缩进一致。
- 列表项尽量保持同一粒度（都是动作、或都是概念）。

## 5. 代码与命令

- 代码块使用三引号并标注语言：
  - Bash：```bash
  - JavaScript：```js
  - TypeScript：```ts
  - YAML：```yaml
  - SQL：```sql
- 命令保持可执行且单行清晰；多步命令用编号列表。
- 示例中的占位值用可读替代，如：`your-domain.com`、`your-password`。
- 不要把整段说明文字放进代码块。

## 6. 引用、提示与注意

- 提示类内容用“提示：”“注意：”开头，放在独立段落。
- 风险/安全相关内容需明确强调。

示例：

注意：生产环境请使用强密码并通过 .env 管理敏感信息。

## 7. 标点与语言

- 中文正文优先使用中文标点。
- 英文单词与中文之间保留空格（如：Docker Compose）。
- 技术名词大小写遵循官方写法（如：Vue、TypeScript、MySQL）。

## 8. Front Matter（文章头信息）

- 若文档位于 `source/_posts/`，保持已有 Front Matter，不随意新增字段。
- 日期格式：`YYYY-MM-DD` 或 `YYYY-MM-DD HH:mm:ss`。
- tags、categories 使用数组或单行规范格式，避免混用空格与缩进。
- 需要根据内容补充tags、categories、description、keywords
- 新增文章**必须**填写 `cover` 字段，使用在线图片 URL（推荐 [picsum.photos](https://picsum.photos)，格式：`https://picsum.photos/id/{唯一ID}/800/450`），确保每篇文章封面图不重复。禁止留空或使用本地路径。

## 9. 图与资源引用

- 图片路径使用相对路径。
- 图片需有简短替代文本。

示例：

![配置界面](image.png)

## 10. 例子与模板一致性

- 例子需贴合正文内容，避免无关代码。
- 统一变量命名风格（如：`userId`、`dbHost`）。
- 优先使用简洁、可理解的示例，不引入复杂依赖。

## 11. 技术介绍类文章风格（精简有力）

技术介绍类文章（工具手册、API 方法、命令参考、框架笔记等）采用“参考手册”式写法，力求精简有力、即查即用。

### 11.1 格式选择（按内容类型自适应）

技术文章不应套用单一模板。写之前先判断话题属于哪种类型，选择最合适的呈现方式：

**类型判断优先级：**

1. **API/命令/语法参考型**（如 Pydantic BaseModel、git 命令、pandas DataFrame 方法）— 核心是"怎么用"，即输入和输出
   - 结构：参数总览 → 返回值说明 → 整合示例
   - **合是默认，拆是例外**：优先用一个带注释的代码块集中展示多个参数的用法；仅当参数间存在正交场景（如"异步 vs 同步""有回调 vs 无回调"）无法在同一示例中说清时才拆分
   - **参数描述**应包含：名称与类型、必填/可选、默认值、可接受的值（枚举可选值）、作用说明。参数较多时用表格呈现
   - **返回值描述**应包含：返回类型、形状或结构、关键属性或方法（如 `.classes_`、`.feature_importances_`）
   - **示例**优先覆盖正常路径（happy path）；边缘情况仅当属于常见陷阱时才补充
   - 参数和返回值用中文描述，代码标识符保持英文

2. **概念/原理/机制型**（如 GC 算法、HTTP 缓存策略、OAuth 流程）— 核心是"理解"
   - 结构：定义 → 原理分层拆解 → 对比表格或流程图描述 → 关键结论
   - 代码可选，仅在能显著提升理解时加入；优先用表格、流程描述、对比图
   - 避免为满足格式要求而硬塞无意义代码

3. **配置/参数/选项型**（如 ESLint 规则、nginx 配置项、Pydantic Field 参数）— 核心是"查选项"
   - 结构：概览表格（参数名/类型/默认值/说明）→ 关键参数展开 → 组合示例
   - 表格优于零散代码块；多个小代码块应合并为一个"典型配置"代码块

4. **对比/选型型**（如 React vs Vue、REST vs GraphQL、p 列 vs m 列 vs b 列）— 核心是"决策依据"
   - 结构：对比表格 → 各自适用场景 → 推荐策略
   - 表格为主，代码为辅

**通用原则：**
- 小片段应考虑整合或用注释串联
- 若能在代码行后使用注释解释清除或补充明白，就无需单独解释代码块
- 如果一个概念用文字+表格就能说清，就不需要代码块
- 代码块的价值在于可复制运行，纯示意性的碎片代码可以精简或合并

### 11.2 写作原则

- **开门见山**：首段直接给定义或核心用途，不写背景铺垫。
- **代码先行**：某些要点可直接在代码的注释中展示，无需单独解释。
- **最小示例**：每个示例只展示一个要点，不堆砌无关参数或上下文。
- **分类呈现**：用“常见模式”“常用示例”“常见约束参数”等分组编排，每组三言两语点明用途，紧跟代码块。
- **避免叙事**：不写“接下来我们…”“让我们看看…”“你可能会遇到…”等引导语或过渡句，直接给结论。
- **精简解释**：代码块后用 1~3 条列表项点明关键参数、易错点或”为什么”（如 `@classmethod 必须加 — 验证器在实例创建之前运行，实例还不存在`），不逐行翻译代码。
- **技巧随功能**：常用技巧、注意点应在功能介绍时及时融入对应小节，避免在末尾设置”常用技巧””注意事项”等专门小节。读者了解功能的同时就能掌握最佳实践，避免阅读割裂。仅当技巧跨越多个功能或模块、无法归入任一具体小节时，才考虑独立成节。

### 11.3 写法对比

**推荐（精简，代码先行，要点列表收尾）：**

```markdown
## 1. BaseModel 基本用法

```python
from pydantic import BaseModel

class User(BaseModel):
    id: int
    name: str
    age: int = Field(ge=0, le=150)

user = User(id=1, name="Alice", age="25")
# age="25" → 自动类型转换为 int 25
```

- 继承 `BaseModel` 即可获得自动验证和类型转换
- 验证失败抛出 `ValidationError`
```

**不推荐（冗余，叙事拖沓，代码后臃肿解释）：**

```markdown
## BaseModel 的基本使用方法

接下来我们来看看如何使用 Pydantic 的 BaseModel。首先需要从 pydantic 导入 BaseModel，
然后定义一个类继承它。在类中，我们使用类型注解来声明字段。下面是一个简单的示例：

```python
from pydantic import BaseModel, Field

class User(BaseModel):
    id: int
    name: str
    age: int = Field(ge=0, le=150)

user = User(id=1, name="Alice", age="25")
print(user.age)  # 输出 25，注意 age 传入了字符串 "25" 但被自动转换为了整数
```

上面的代码展示了 BaseModel 的基本用法。可以看到 Pydantic 会自动进行类型转换，
将字符串 "25" 转换为整数 25。如果传入的数据不符合类型要求，Pydantic 会抛出
ValidationError 异常，这样我们就可以在开发过程中尽早发现数据问题。
```